# Omakase Ruby styling for Rails
inherit_gem: { rubocop-rails-omakase: rubocop.yml }

# Migrations are historical artifacts — don't let cosmetic cops rewrite
# what already ran in production.
Layout/SpaceInsideArrayLiteralBrackets:
  Exclude:
    - 'db/migrate/**/*.rb'
