<div class="help-page">
  <h1 class="help-page__title" data-testid="page-title">MCP Integration Guide</h1>
  <p class="help-page__subtitle">Connect your AI agent to Screenote for visual feedback loops.</p>

  <%# ── Quick Start ── %>
  <section class="help-section">
    <h2 class="help-section__heading">Quick Start</h2>
    <ol class="help-steps">
      <li class="help-steps__item">
        <strong>Install the plugin</strong> — Run the commands below in Claude Code to add the Screenote plugin.
      </li>
      <li class="help-steps__item">
        <strong>Authorize</strong> — On first use, a browser window opens to connect your Screenote account.
      </li>
      <li class="help-steps__item">
        <strong>Use the tools</strong> — Your AI agent can now upload screenshots, read annotations, and resolve feedback.
      </li>
    </ol>
  </section>

  <%# ── Setup ── %>
  <section class="help-section">
    <h2 class="help-section__heading">Setup</h2>
    <div class="help-setup">
      <div class="help-setup__label">Claude Code Plugin</div>
<pre class="help-setup__code"><code>/plugin marketplace add ivankuznetsov/claude-screenote
/plugin install claude-screenote@screenote-marketplace</code></pre>
    </div>
    <p class="help-setup__note">
      After installing, use <code>/screenote</code> to capture pages and <code>/screenote feedback</code> to retrieve annotations.
    </p>
  </section>

  <%# ── Tools Reference ── %>
  <section class="help-section">
    <h2 class="help-section__heading">Tools Reference</h2>
    <div class="tool-grid">
      <% @tools.each do |tool| %>
        <% schema = tool.input_schema_to_json %>
        <% properties = schema[:properties] || {} %>
        <% required_params = schema[:required]&.map(&:to_s) || [] %>
        <div class="tool-card" data-testid="tool-card">
          <div class="tool-card__name"><%= tool.tool_name %></div>
          <div class="tool-card__description"><%= tool.description %></div>
          <% if properties.any? %>
            <div class="tool-card__params">
              <div class="tool-card__label">Parameters</div>
              <ul class="tool-card__params-list">
                <% properties.each do |param_name, param_meta| %>
                  <li>
                    <code><%= param_name %></code>
                    <% if required_params.include?(param_name.to_s) %>
                      <span class="tool-card__required">required</span>
                    <% end %>
                    &mdash; <%= param_meta[:description] %>
                  </li>
                <% end %>
              </ul>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </section>

  <%# ── Workflows ── %>
  <section class="help-section">
    <h2 class="help-section__heading">Workflows</h2>

    <div class="help-workflow">
      <h3 class="help-workflow__title">Capture &amp; Annotate</h3>
      <p class="help-workflow__description">Human uploads a screenshot, annotates it, and the AI agent picks up the feedback.</p>
      <ol class="help-workflow__steps">
        <li>Human uploads a screenshot in the Screenote UI</li>
        <li>Human draws annotations and leaves comments</li>
        <li>Agent calls <code>list_screenshots</code> to discover new screenshots</li>
        <li>Agent calls <code>list_annotations</code> to read feedback</li>
        <li>Agent calls <code>get_annotation</code> to see the cropped visual context</li>
        <li>Agent implements the fix and calls <code>resolve_annotation</code></li>
      </ol>
    </div>

    <div class="help-workflow">
      <h3 class="help-workflow__title">Agent Feedback Loop</h3>
      <p class="help-workflow__description">The AI agent captures its own work, uploads it, and waits for human review.</p>
      <ol class="help-workflow__steps">
        <li>Agent takes a screenshot of localhost (e.g. with Playwright)</li>
        <li>Agent calls <code>create_screenshot_upload</code> to get a signed upload URL</li>
        <li>Agent uploads the image directly via the signed URL</li>
        <li>Human opens the annotate URL and leaves visual feedback</li>
        <li>Agent calls <code>list_annotations</code> to read comments</li>
        <li>Agent reads each annotation with <code>get_annotation</code></li>
        <li>Agent fixes issues, re-screenshots, and repeats</li>
      </ol>
    </div>
  </section>
</div>
